{
  "name": "shingo-sf-api",
  "version": "1.0.0",
  "description": "Interface with our SF instance.",
  "main": "index.js",
  "dependencies": {
    "@types/jsforce": "^1.8.12",
    "@types/node": "^10.3.1",
    "@types/winston": "^2.3.9",
    "deep-cleaner": "^1.1.2",
    "grpc": "^1.12.2",
    "jsforce": "^1.8.4",
    "typescript": "^2.9.1",
    "winston": "^2.4.0"
  },
  "scripts": {
    "start": "node build/index.js",
    "start:dev": "nodemon -e ts --exec \"npm run compile\"",
    "proto": "mkdir -p build/proto && cp src/microservices/*.proto build/proto/",
    "compile": "npm run proto && tsc -p tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Dustin E. Homan",
  "license": "MIT"
}
